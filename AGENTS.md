# Codex project instructions

This repository is one part of the three-repository pgCodeKeeper toolchain.
Before substantive work, read `../CLAUDE.md` completely and account for
affected sibling repositories. Do not edit siblings unless the task requires it.

The parent file is outside this Git root and is not inherited automatically.
Validate local Graphify hook state before relying on generated graph data.
